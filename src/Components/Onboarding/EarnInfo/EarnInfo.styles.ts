import styled from 'styled-components'
import { Link } from 'wouter'

export const AccessKeyField = styled.textarea`
  width: 90%;
  height: 160px;
  padding: 10px;
  background-color: rgba(0, 0, 0, 0.05);
`
